# 更新日志

## 4.1.0

### 账号登录

- 设置页新增 MISSAV 注册邮箱与密码登录，可直接连接当前访问线路的站点账号。
- 密码只用于本次登录请求，不会保存；登录成功后仅将站点会话保存在系统钥匙串中。
- 登录成功后可在资料库读取网站收藏，并继续使用详情页的网站收藏操作。

### 访问线路验证

- 新增“验证访问线路”入口，可先在网页中完成 Cloudflare 验证，再返回设置页填写邮箱和密码。
- 访问线路验证与账号登录分开处理，无需在验证网页中登录账号。
- 保留账号状态验证、登录失效提示和退出站点账号操作。

### 会话与资料库

- 站点会话按当前访问域名保存，不同访问线路分别管理账号状态。
- 登录或退出后会刷新网站收藏状态，避免继续显示旧账号内容。
- 网站收藏与本机收藏、播放记录和浏览记录继续分开管理。

## 4.0.0

### 原生 iOS 内容界面

- 重新梳理页面层级、内容间距与操作顺序，使浏览和观看路径更加清晰。
- 首页改为继续观看优先的内容结构，移除同权重的大型彩色功能卡，并统一今日热门与最近更新货架。
- 搜索分类入口改为紧凑信息块；搜索结果新增列表与大封面两种显示方式，并可保留用户的显示偏好。
- 浏览页仅在第一页显示精选主视觉，后续页面保持一致的内容网格；当前栏目同时使用勾选、字重和辅助功能状态表达。
- Home Tab 优先展示继续观看，并提供浏览、搜索、资料库和推荐入口。

### 详情、资料库与状态

- 详情页保留单一主要播放操作，将本机收藏和网站收藏收口为同级次要操作，并使用肯定式状态文案。
- 资料库改为“收藏 / 历史”第一层与“本机 / 网站、播放 / 浏览”第二层，改善窄屏和系统大文字下的可读性。
- 统一媒体封面、标题、元数据、推荐理由、状态图标与尾部操作的对齐轴和几何规格。
- 完善加载、空内容、部分更新失败、收藏不可用和播放源不可用等状态的正式说明与恢复路径。
- 设置页移除版本营销内容，保留站点、网站账号、数据与隐私、关于和更新日志等长期信息。

### 无障碍与发布质量

- 页面信息与媒体文字使用左侧阅读轴；空状态、加载状态、分页页码和按钮内部按语义居中；尾部操作保持独立尾轴与整行中线。
- 关键选中、收藏、账号和错误状态同时使用文字、图标、形状或进度，不仅依赖颜色。
- 全项目 TypeScript diagnostics、项目检查和媒体源回归测试通过。

## 3.4.0

### 首页与产品界面

- “最近播放”改为与“热门推荐”“最近更新”一致的横向内容货架，统一标题、副标题、左侧轴线和卡片节奏。
- 首页远程推荐加载改为紧凑行反馈，本机已有最近播放内容时不再出现大面积空白占位。
- 详情页本机收藏与网站收藏改为全宽纵向操作，提升窄屏、长文案和系统大文字下的可读性与点击空间。
- Scripting Home Tab 在真实滚动容器上隐藏顶部 scroll edge effect，减少 iOS 26 滚动和回弹时的横向模糊条。
- 优化列表、媒体封面和加载状态的层级，减少重复内容与不必要的界面开销。

### 播放源可靠性

- 排除推荐卡片的 `preview.mp4`、Magnet 下载地址、模板变量、`null`、`undefined` 和路径不是真实媒体文件的伪来源。
- 只接受 URL 路径真正以 `.m3u8` 或 `.mp4` 结尾的资源，并按协议、主机和真实资源路径去重，临时 token 不再造成重复画质。
- 每个固定清晰度只保留一个入口；自动清晰度全局只保留一个并统一显示为“自动清晰度”。
- 增强 `1080p`、`1080.m3u8`、`height=1080`、`1920x1080` 等清晰度识别。
- 刷新播放地址时优先匹配同一资源路径；多个同类型同清晰度候选存在时不再任意误选，也不会在 HLS 与 MP4 之间静默跨类型切换。

### 性能与稳定性

- 消除独立首页首次挂载时的重复本机资料库查询，并在 SQLite 查询层限制首页收藏读取数量。
- 网站收藏改为仅在进入“网站收藏”资料库分类或主动刷新时按需加载，避免本机收藏、浏览或播放变化触发无关 WebView。
- 未登录或登录已失效时，详情页不再启动网站收藏状态 WebView，减少重复页面加载、内存峰值和网络消耗。
- Home Tab 加入 latest-request-wins 请求代次保护，旧请求不再覆盖新结果。
- Home Tab 热门内容增加 90 秒短期缓存；普通重新选中只在过期时刷新，下拉刷新仍会强制获取最新内容。
- 优化 MISSAV 视频卡片解析，处理完同一作品的连续链接后直接跳过已消费链接，减少重复 URL 解码和正则扫描。

### 发布信息

- 脚本版本、设置页、应用内更新日志和项目更新日志统一更新为 3.4.0。
- 新增媒体源解析回归测试，覆盖预览视频排除、资源去重、自动画质收口、同路径刷新和歧义拒绝。

## 3.3.0

### 全局产品精修

- 完成首页、发现、搜索、资料库、详情、设置、账号和播放器入口的全局产品审计。
- 统一分页、加载、错误、空状态和刷新后的内容层级，减少页面状态跳变和重复提示。
- 统一浅色与深色模式下的背景、文字层级、强调色和媒体间距。
- 移除所有封面上的播放小图标叠加，保留列表操作行中的明确播放入口，提升封面信息可读性。
- 优化长标题、长错误信息、窄屏和较大文字显示下的行高与文本约束。
- 关键状态继续同时使用文字、图标、形状和可访问性描述，不依赖单一颜色区分。

### 分页与浏览体验

- 发现页、搜索结果页和标签搜索页统一支持分页成功后自动回到顶部浏览。
- 上一页、下一页、分类切换、筛选切换和排序切换均会更新顶部浏览位置。
- 修复从第二页返回第一页时仍停留在原滚动位置的问题。
- 修复第一页更换搜索词或分类后未回到顶部的问题。
- 保留网站收藏“加载更多并追加内容”的当前位置，避免用户加载下一批后丢失浏览上下文。
- 优化分页请求期间的加载状态和旧结果保护，避免新搜索标题与旧结果混杂。
- 修复提交空搜索后旧请求仍可能回写结果的竞态。

### 详情页与收藏

- 重新整理详情页信息层级：封面、番号、标题、演员、元数据、播放和双收藏操作路径更加清晰。
- 本机收藏与网站收藏改为并列且明确区分的独立操作，不互相覆盖状态。
- 本机收藏读取失败时不再伪装成“未收藏”，按钮会进入不可用状态并给出明确原因。
- 网站收藏读取中、已收藏、未收藏、登录失效和接口失败状态均提供对应文字与辅助功能描述。
- 播放和其他清晰度正在打开时同步更新按钮文案与 VoiceOver 状态。
- 详情长标题增加可见空间，作品信息区域保留完整标题展示路径。
- 保留标签点击快速搜索、真实网站收藏接口确认、历史记录写入和其他清晰度播放能力。

### 播放体验

- 保留单层系统播放器，避免重复全屏导致的白边、无法退出和播放异常。
- 播放器支持系统画中画、系统控制和旋转能力，保持播放前重新获取最新播放源。
- 竖屏播放时保持顶部系统状态栏显示。
- 明确系统 `AVPlayerView` 的控制层边界：底部系统播放控制由系统管理，不强行叠加会影响播放稳定性的自定义控制壳。
- 播放失败、播放源失效和网络异常均保留清晰的失败反馈。

### 账号、资料库与设置

- 网站账号继续使用站点隔离的 Keychain 会话，不保存账号密码。
- 完整恢复 WebView Cookie 会话，保留 HttpOnly、Secure、domain、path 和有效期信息。
- 网站收藏、收藏状态读取和详情页收藏操作统一复用当前站点登录会话。
- 设置页登录、重新验证和账号异常增加明确错误反馈，避免操作失败时无提示。
- 设置页版本信息更新为 3.3.0，发布元数据与页面显示保持一致。
- 新增脚本内原生更新日志页面，无需跳转外部文档即可查看当前版本与主要历史版本。
- 设置页“关于”区域增加常驻更新日志入口，用户可随时查看版本历史。
- 资料库继续区分网站收藏、本机收藏、播放记录和浏览记录；本机记录不会上传。

### 发布质量

- 全项目 TypeScript diagnostics 通过。
- `scripting-ts project \"MISSAV\" --check` 通过。
- 全项目未发现 `playIndicator` 封面叠加参数。
- 完成全项目界面代码检查与关键页面回归。
- 全项目未发现参考项目名称泄漏。

## 3.2.0

### 网站账号与真实收藏

- 新增 MISSAV 官网账号登录，在脚本内的网站窗口中完成登录，不保存账号密码。
- 使用 Keychain 按站点域名安全保存登录会话，`missav.ai` 与 `missav.ws` 的账号状态完全隔离。
- 支持完整 WebView Cookie 会话恢复，包括 HttpOnly、Secure、domain、path 和有效期信息。
- 增加账号状态验证，可区分未登录、登录失效、网站验证和网络加载失败。
- 重新验证和退出网站账号纳入设置页，退出只清除本机保存的会话，不修改网站账号内容。

### 网站收藏同步

- 新增资料库“网站收藏”，读取当前网站账号的真实收藏，和本机收藏、播放记录、浏览记录分开管理。
- 网站收藏支持分页加载、刷新和空收藏状态反馈。
- 详情页拆分“本机收藏”和“网站收藏”两个独立操作，不会互相覆盖或同步到错误位置。
- 网站收藏使用 MISSAV 真实收藏接口完成加入与取消，并在操作后重新读取服务器状态确认结果。
- 网站收藏状态从详情页真实接口和页面结构读取，不根据番号猜测网站内部作品标识。
- 收藏操作提供执行中、成功、未登录、登录失效、网站验证、接口变化和失败反馈；服务器未确认时不会更新界面状态。

### 账号会话与可靠性

- 账号窗口、收藏资料库和详情页统一使用当前站点会话，减少浏览器验证状态丢失。
- 修正已登录页面保留隐藏登录表单时的账号误判。
- 修正 MISSAV 已登录接口返回空用户字段时被误判为匿名账号的问题。
- 网站收藏状态读取失败时显示明确错误，不再永久停留在“读取网站收藏状态”。

## 3.0.0

- 全新首页汇集继续观看、个性推荐、收藏、热门内容与最近更新，更快找到想看的内容。
- 发现页新增栏目快捷选择、精选内容、类型与排序选项，并以自适应网格呈现更多作品。
- 资料库整合收藏、播放记录与浏览记录，支持快速切换和分别清理历史。
- 搜索首页新增热门内容与分类入口；支持番号、女优和标题搜索，以及结果翻页。
- 搜索时可通过拖动页面、点击输入框外或提交搜索收起键盘，浏览过程更加顺畅。
- 详情页重新整理封面、作品信息、播放、收藏、标签与画质选项，长标题和多项信息更易阅读。
- 作品标签支持快速查找相关内容，并可继续进入详情和播放。
- 播放前自动获取当前可用画质，保留系统播放器与画中画体验。
- 推荐内容根据保存在本机的收藏、浏览和播放记录生成，个人记录不会上传。
- 支持将 MISSAV 设为 Scripting 的 Home Tab，直接访问发现、搜索、资料库、推荐、热门内容和设置。
- 统一媒体封面、列表、网格、加载、空状态与错误反馈，改善窄屏和长标题显示。
- 优化弱网体验：刷新失败时尽可能保留上次成功加载的内容。
- 所有主要操作均提供文字、图标或状态说明，并完善 VoiceOver 与色觉无障碍体验。
- 自动适配系统浅色与深色模式。

## 2.1.0

- 更新视频播放入口页：大幅封面、清晰的作品信息、主播放按钮与独立收藏操作形成明确的观看路径。
- 作品标题、演员、番号、日期与时长重新编排；作品标签和其他清晰度下移为轻量内容分区，清晰度行使用整行分隔线。
- 播放与收藏操作补充执行中状态、重复点击保护和失败反馈；详情请求增加 latest-request-wins 防护。
- 完成全部生产页面的 surface、颜色、分组、间距与分隔线审计，统一系统浅深色下的视觉层级。
- 移除整页空状态、资料库统计和普通内容区中过重的灰色卡片，改用环境背景、轻量系统填充与内容层级表达分组。
- 搜索、推荐、收藏、播放历史和浏览历史统一使用共享纵向媒体列表，所有同类行之间采用整行分隔线。
- 资料库三个完整记录入口改为全宽行间分隔；统计摘要改为紧凑无背景布局，消除重复大色块。
- 搜索框、浏览筛选、媒体占位与详情封面统一采用更轻的系统填充，保持 Increase Contrast 与自动深浅色兼容。
- 统一纵向缩略图为 16:9，收紧横向媒体卡宽度并固定双行标题占位，改善货架对齐和手机信息密度。
- 危险清空区减少重复留白，确认项明确区分播放历史与浏览历史，并补充完整 VoiceOver 后果说明。
- 修正搜索重复提交与新关键词加载期间旧标题错配；同步设置页站点术语和版本文案。
- 统一系统控件、页面背景与内容层级，保持各页面体验一致。

## 2.0.3

- 移除资料库全部入口区域的大面积灰紫背景，改为系统环境背景上的独立行与轻量分隔线。
- 资料库入口图标与文字改用中性语义色，仅在真实选中或危险状态使用强调色。
- 统计卡、浏览筛选和搜索输入改用更轻的系统填充，降低浅色模式中的色块感。
- 清空菜单触发行移除大块背景，只保留自然页面层级、分隔线和系统原生菜单。

## 2.0.2

- 播放历史与浏览历史的清空操作改为系统原生整行 `Menu` 选择设计。
- 菜单内部提供带垃圾桶图标的“确认清空”和独立“取消”选项，降低误操作风险。
- 清空记录改为整行原生菜单，明确提供取消与确认操作，并扩大可点击区域。
- 优化详情页画质选项，仅有一个可用画质时不再显示空的“其他清晰度”区域。

## 2.0.1

- 修复浅色模式下导航栏与内容区域使用不同系统背景造成的顶部颜色断层。
- 统一应用导航结构，修复工具栏丢失、重复和双层显示。
- 关闭、设置与最小化操作统一显示在根工具栏。

## 2.0.0

- 更新为内容优先的媒体浏览界面，新增精选内容、横向推荐和自适应内容网格。
- 主导航调整为首页、浏览、资料库和搜索；低频设置迁移为首页二级入口。
- 统一详情页跳转方式，减少重复导航状态并改善长列表性能。
- 首页整合今日热门、最近更新、最近播放和收藏内容，浏览页移除重复搜索入口并使用紧凑筛选菜单。
- 资料库升级为统计概览、收藏/播放/浏览货架和完整记录入口。
- 详情页改为大幅主视觉、单一主要播放操作、收藏状态与其他清晰度列表。
- 全面使用系统语义背景与文字颜色，自动跟随系统浅色和深色外观，不强制固定黑色背景。
- 长列表与横向货架改用懒加载容器，自适应网格降低无效视图构建。
- 数据库改为 UI 呈现后异步预热；详情保存与浏览记录合并为单次视频写入。
- 推荐内容增加短时缓存和请求复用，减少重复加载。
- 启用完整最小化生命周期，并将关闭动作收口为 presentation dismiss。

## 1.1.0

- 增加 MISSAV 域名切换，支持 `missav.ws` 和 `missav.ai`。
- 增加本地 SQLite 资料库，保存收藏、浏览历史和播放历史。
- 自动迁移旧版收藏与播放记录，保留已有本地数据。
- 增加基于本地收藏、浏览和播放行为的影片推荐。
- 增加推荐入口、浏览历史入口和更完整的资料库页面。
- 优化浏览、搜索、详情、收藏、历史和设置页面的内容层级与交互反馈。
- 播放历史改为在播放器打开前记录。
- 移除播放界面的标题信息 HUD，保留系统播放器控制。
- 修复设置页资料库入口无响应的问题。
